# Server configuration
PORT=5001

# MongoDB Atlas connection string (note /ChatMind before query params)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/ChatMind?retryWrites=true&w=majority
